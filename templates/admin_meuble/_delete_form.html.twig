<form method="post" action="{{ path('app_admin_meuble_delete', {'id': meuble.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meuble.id) }}">
    <button class="btn">Delete</button>
</form>
